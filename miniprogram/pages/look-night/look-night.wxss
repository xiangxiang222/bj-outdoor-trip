@import "../settings/settings.wxss";
.sub { margin-top: 8rpx; color: var(--muted, #8a8f99); font-size: calc(24rpx * var(--ui-scale)); }
.mark { color: #34c759; font-size: calc(36rpx * var(--ui-scale)); }
.list { margin-top: 24rpx; }
